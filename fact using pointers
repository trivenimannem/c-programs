#include <stdio.h>
void factorial(int n, int *fact){
    *fact=1;
    for(int i=1;i<=n;i++)
        *fact*=i;
}
int main(){
    int n,f;
    int *ptr=&f;
    scanf("%d",&n);
    factorial(n,ptr);
    printf("Factorial: %d",*ptr);
    return 0;
}
