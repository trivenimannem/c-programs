#include <stdio.h>
int multiply(int a, int b)
{
    if(b==0)
        return 0;
    else if(b>0)
        return a + multiply(a,b-1);
    else
        return -multiply(a,-b);
}
int main(){
    int a,b;
    scanf("%d%d",&a,&b);
    printf("Product: %d",multiply(a,b));
    return 0;
}
